/target
*.spkg

# Abigen output, regenerated by build.rs from abi/*.json on every build. src/abi/mod.rs is ours and is
# committed; everything beside it is generated.
/src/abi/*.rs
!/src/abi/mod.rs

# Secrets: The Graph Market key and anything else the sink needs.
.env
build/
